Help for SET PRIVATE:

SET PRIVATE causes Services to use private messages
when talking to the user authenticated on this account.

Syntax: SET PRIVATE ON|OFF

Example:
    /msg &nick& SET PRIVATE ON
