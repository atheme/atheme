Help for TAXONOMY:

The TAXONOMY command lists metadata information associated
with registered users.
#if module nickserv/set_property

This information can be added and removed with the
SET PROPERTY command.
#endif

Example:
    /msg &nick& TAXONOMY foo
