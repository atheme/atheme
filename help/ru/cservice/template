ѕомощь по TEMPLATE:

 оманда TEMPLATE позвол€ет организовывать систему шаблонов
- предварительно заданых наборов флагов, упрощающих
использование команды FLAGS.

»спользованна€ без аргументов, команда выведет список
глобальных шаблонов сети.
ƒанный список включает в себ€ SOP/AOP/HOP/VOP шаблоны.

—интаксис: TEMPLATE

≈сли в качестве первого аргумента указать название канала,
выведет список шаблонов указанного канала.

—интаксис: TEMPLATE <#канал>

”казание еще двух параметров дл€ команды позволит вам измен€ть
и/или создавать новые шаблоны. ¬ качестве изменени€ может быть
указано название шаблона (копирует шаблон) или измен€емый флаг
(начинающийс€ с + или -, и знака '!' в качестве доп. услови€).
Ўаблоны не могут быть пустыми, то есть не содержать никаких
записей, указание шаблона подобным образом - удалит его.

≈сли указываютс€ флаги с префиксом '!', все записи в списке
доступа, совпадающие с указанным шаблоном, будут изменены
соответственно (буду удалены/добавлены измененные флаги,
за исключением флага +F - его нельз€ удалить).

 оличество шаблонов дл€ канала имеет свои ограничени€.

ƒл€ использовани€ данной команды необходимы права доступа,
аналогичные схеме разделени€ прав используемой дл€ команды
FLAGS (см. HELP FLAGS) 

—интаксис:
    TEMPLATE <#канал> [шаблон старый_шаблон]
    TEMPLATE <#канал> [шаблон измен€емые_флаги]
    TEMPLATE <#канал> [шаблон !измен€емые_флаги]

Examples:
    /msg &nick& TEMPLATE #foo
    /msg &nick& TEMPLATE #foo user VOP
    /msg &nick& TEMPLATE #foo user !+A
    /msg &nick& TEMPLATE #foo co-founder +*-OH
    /msg &nick& TEMPLATE #foo op -*+vVhoti
    /msg &nick& TEMPLATE #foo obsoletetemplate -*
